export enum EAccountType {
	USER = 'USER',
	ADMIN = 'ADMIN',
}

export enum ECapacityUnit {
	l = 'l',
	ml = 'ml',
}

export enum EGender {
	male = 'Male',
	female = 'Female',
	all = 'All',
}

export enum EProductStatus {
	active = 'ACTIVE',
	stopSelling = 'STOP_SELLING',
}

export enum EUserRole {
	user = 'USER',
	admin = 'ADMIN',
	subAdmin = 'SUB_ADMIN',
}

export enum EOrderStatus {
	pending = 'PENDING',
	delivery = 'DELIVERY',
	completed = 'COMPLETED',
	cenceled = 'CENCELED',
}
